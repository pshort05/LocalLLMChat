feat: chat history, dark mode modals, save config, system info, docs

- Add chat history browser: auto-save after every reply, load/delete individual or all conversations
- Model-agnostic history: conversations stored with origin_model metadata; any model can continue any chat
- Upsert save pattern: client tracks currentConversationId to update rather than duplicate
- Fix dark mode modals (Chat History, Setup LLM): deep blue backgrounds matching theme
- Fix placeholder text readability in dark mode (lighter grey, opacity:1)
- Add Save Configuration button: explicitly saves all GUI settings with visual confirmation
- Add System Info feature: /api/system_info endpoint detects CPU, RAM, GPU/VRAM; UI modal shows hardware tier recommendation
- Add _get_cpu_name(), _get_ram_info(), _get_gpu_info(), _get_system_info() backend methods
- GPU detection: nvidia-smi → rocm-smi → system_profiler (macOS) → lspci fallback
- Remove duplicate local imports of platform/subprocess from method bodies
- Update install-macos.sh: interactive model selection, llama3.2 default, update-models.sh install step
- Update install-windows.ps1: same model selection improvements, remove dolphin-mistral default
- Add MODELS.md: quantization guide, hardware tiers, Ollama/LM Studio deep-dive, model recommendations
- Update README.md: documentation table, chat history section, sidebar reference, token usage, API table, MODELS.md link
